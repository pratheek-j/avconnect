# Rust build artifacts
target/
# rustfmt backup files (when --backup is used)
**/*.rs.bk
*.pem
.DS_Store

#cursor files
.cursor/

# Legacy artifacts (safe to delete)
node_modules/
dist/
bin/
builds/
config
credentials
credentials.json
servers.json
region.json
goAws
run.sh
